<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.productions.rk.tickitock">
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeScreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OptionScreen" />
        <activity android:name=".TwoPlayerSameDevice" />
        <activity android:name=".SinglePlayerSuperEasy" />
        <activity android:name=".SinglePlayerEasy" />
        <activity
            android:name=".StatsActivity"
            android:label="@string/title_activity_stats"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".StatsActivity2"
            android:label="@string/title_activity_stats2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BluetoothProcessHandling"></activity>
    </application>

</manifest>
